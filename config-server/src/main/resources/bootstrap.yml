spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/koshishrijal/event-driven-microservice-arch-config-server.git
          default-label: master
          username: koshishrijal
          password: koshish2018424
          clone-on-start: true
    fail-fast: true
  security:
      user:
        name: admin
        password: admin
